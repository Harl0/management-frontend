play.filters.headers.contentSecurityPolicy = "default-src 'self' 'unsafe-inline' 'unsafe-eval' code.jquery.com maxcdn.bootstrapcdn.com ajax.googleapis.com data:"
play.http.secret.key=abcdefghijk

app.name = "management-frontend"

client {
//  url = "http://client.private"
  url = "http://localhost:9999"
}

token {
  url = "http://token.private"
}
